<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="holiday_month_action" model="ir.actions.act_window">
        <field name="name">holiday month Action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">holiday.month.date</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
          <p class="oe_view_nocontent_create">
            <!-- Add Text Here -->
          </p><p>
            <!-- More details about what a user can do with this object will be OK -->
          </p>
        </field>
    </record>
    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="holiday_month_date" name="Set Holiday" parent="order_food_menu" action="holiday_month_action" sequence="3"/>
</odoo>